Implement as "prompt" based agent

Create a search result, based on your input. Just mock a prodcut based on input params or, randomly decide if you say no response.

The result should always be in the form {"name": "text", "price": 0.0€, "description": "text"}